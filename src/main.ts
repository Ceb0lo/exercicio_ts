function multiplicacaoA(numeroA:number, numeroB: number){
    return numeroA * numeroB
}

const multiplicacaoB = (numeroA:number, numeroB: number): number => numeroA * numeroB

function dizOlaA(nome:string) {
    return `Olá ${nome}`
}

const dizOlaB = (nome:string):string => `Olá ${nome}`